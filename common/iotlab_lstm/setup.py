from setuptools import setup, find_packages

setup(
    name="IoTLab LSTM",
    author="Group4",
    description="Long short-term memory model for training and predicting",
    version="0.1",
    packages=find_packages(),
    zip_safe=False,
)
