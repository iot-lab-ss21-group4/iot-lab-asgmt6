from setuptools import setup, find_packages

setup(
    name="IoTLab SARIMAX",
    author="Group4",
    description="SARIMAX model for training and predicting",
    version="0.1",
    packages=find_packages(),
    zip_safe=False,
)
